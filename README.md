# Quantum Mini-PCI Timecard - Документация

Полная документация для работы с Quantum Mini-PCI Timecard на базе Shiwa Time.

## ⚠️ Важное примечание

На устройстве может быть установлено программное обеспечение как **Timebeat**, так и **Shiwa Network** (Shiwa Time).

**В данной документации рассматривается только работа с Shiwa Time.**

---

## 📚 Содержимое

### 📖 Руководства

1. **[SHIWATIME_GUIDE.md](SHIWATIME_GUIDE.md)** - Основное руководство пользователя
   - Подключение по SSH
   - Управление сервисом Shiwa Time
   - Редактирование конфигурации
   - Настройка PTP Grandmaster
   - Настройка сети (NetworkManager)
   - Учетные данные и безопасность
   - Мониторинг и диагностика

2. **[shiwatime_config_guide.md](shiwatime_config_guide.md)** - Подробное руководство по конфигурации
   - Полное описание всех параметров конфигурационного файла
   - Настройка PTP, NTP, PPS, GNSS
   - Алгоритмы синхронизации
   - Clock Quality настройки
   - Примеры конфигураций для разных сценариев
   - Команды для проверки и отладки

3. **[CUSTOM_BOOT_GUIDE.md](CUSTOM_BOOT_GUIDE.md)** - Настройка загрузочного экрана
   - Текстовый логотип (ASCII Art)
   - Plymouth Splash Screen
   - Графический логотип ядра
   - Настройка MOTD
   - Готовые примеры логотипов

4. **[MONITORING.md](MONITORING.md)** - ⭐ Быстрый доступ к веб-мониторингу
   - Grafana dashboard (http://90.156.231.174)
   - Elasticsearch API
   - Основные метрики и нормальные значения
   - Быстрая диагностика проблем
   - Типичные проблемы и решения

5. **[GRAFANA_REBRAND_COMPLETE.md](GRAFANA_REBRAND_COMPLETE.md)** - 🎨 Отчет о ребрендинге Grafana
   - Timebeat → Shiwa Time
   - Список изменений
   - Новые учетные данные
   - Инструкция по восстановлению из бэкапа

6. **[GUI_CLI_SWITCH_GUIDE.md](GUI_CLI_SWITCH_GUIDE.md)** - 🖥️ Переключение между CLI и GUI режимами
   - Переключение сервера мониторинга (90.156.231.174)
   - Переключение Raspberry Pi TimeCard Mini (192.168.16.238)
   - Настройка VNC для удаленного доступа
   - Запуск ShiwaDiffPHC на сервере
   - X11 Forwarding для GUI приложений

7. **[CLONING_GUIDE.md](CLONING_GUIDE.md)** - 🔄 Клонирование и портирование системы
   - Поиск устройства в сети (автоматический и ручной)
   - Создание полного образа SD-карты
   - Создание архива конфигурации
   - Восстановление из образа
   - Настройка после клонирования
   - Автоматические скрипты для клонирования

### ⚙️ Конфигурационные файлы

7. **[shiwatime_ru.yml](shiwatime_ru.yml)** - Русифицированный конфигурационный файл
   - Все комментарии переведены на русский язык
   - Активно используется на устройстве: `/etc/shiwatime/shiwatime.yml`

8. **[shiwatime.yml](shiwatime.yml)** - Оригинальный конфигурационный файл
   - Английская версия (бэкап)
   - Скачан с устройства для справки

### 🔧 Вспомогательные файлы

9. **[check_ptp_config.sh](check_ptp_config.sh)** - Скрипт проверки PTP конфигурации
   - Проверка настроек clock_quality
   - Статус сервиса shiwatime
   - Открытые PTP порты (319, 320)
   - PTP announce сообщения
   - Последние логи сервиса

10. **[shiwatime.service](shiwatime.service)** - Systemd unit файл
    - Файл сервиса для systemd
    - Расположение на устройстве: `/lib/systemd/system/shiwatime.service`

11. **[UBX_MONITORING_GUIDE.md](UBX_MONITORING_GUIDE.md)** - 📡 Руководство по мониторингу и настройке UBX команд
    - Мониторинг UBX команд, отправляемых на модуль Ublox
    - Настройка длительности импульса PPS (5 мс вместо 100 мс)
    - Использование скриптов для конфигурации модуля Ublox

12. **[monitor_ubx_commands.py](monitor_ubx_commands.py)** - Скрипт мониторинга UBX команд
    - Отслеживание всех UBX команд на последовательном порту
    - Детальный анализ CFG-TP5 (Time Pulse) сообщений
    - Вывод длительности импульса и параметров конфигурации

13. **[configure_ublox_timepulse.py](configure_ublox_timepulse.py)** - Скрипт настройки длительности импульса PPS
    - Установка длительности импульса на 5 мс (или другое значение)
    - Конфигурация через протокол UBX (CFG-TP5)
    - Поддержка параметров задержки кабеля и других настроек

14. **[configure-ublox-pulse-early.service](configure-ublox-pulse-early.service)** - Systemd service для настройки (временное решение)
    - Попытка установки длительности импульса 5 мс ДО запуска shiwatime
    - **⚠️ ОГРАНИЧЕНИЕ:** Shiwatime перезаписывает настройки при старте, поэтому это решение не решает проблему полностью
    - Использует скорость 9600 baud для Raspberry CM4
    - См. [SOLUTION_SUMMARY.md](SOLUTION_SUMMARY.md) для подробного описания проблемы

15. **[SOLUTION_SUMMARY.md](SOLUTION_SUMMARY.md)** - 📋 Резюме проблемы и решений
    - Подробное описание проблемы с блокировкой порта и перезаписью настроек
    - Анализ доступных решений
    - Рекомендации по решению проблемы

16. **[CONFIG_CHECK.md](CONFIG_CHECK.md)** - 🔍 Проверка конфигурации shiwatime
    - Результаты проверки конфигурационного файла на наличие параметров длительности импульса
    - Подтверждение отсутствия параметра в конфигурации
    - Рекомендации по использованию Timebeat CLI

17. **[CLI_CHECK_GUIDE.md](CLI_CHECK_GUIDE.md)** - 📟 Руководство по проверке Timebeat CLI
    - Инструкции по подключению к CLI интерфейсу
    - Ссылка на официальную документацию команд CLI
    - Руководство по поиску команд для настройки timepulse
    - Шаблон для отчетности о результатах проверки

18. **[NTP_SERVER_SETUP.md](NTP_SERVER_SETUP.md)** - 🕐 Настройка NTP сервера
    - Инструкции по настройке NTP сервера в shiwatime
    - Конфигурация для раздачи времени в локальную сеть
    - Проверка работы и настройка клиентов
    - Диагностика проблем

### 📋 Встроенные разделы

- **🔒 Безопасность** - Смена паролей, SSH ключи, файрвол
- **📦 Резервное копирование** - Автоматические бэкапы, восстановление
- **⚡ Оптимизация производительности** - Настройки для максимальной точности
- **🔄 Обновления системы** - Обновление Raspberry Pi OS и Shiwa Time
- **❓ FAQ** - Часто задаваемые вопросы и решения

---

## 🚀 Быстрый старт

### 1. Подключение к устройству

#### Поиск устройства в сети

Если IP-адрес устройства неизвестен, используйте один из способов:

**Способ 1: Сканирование локальной сети**
```bash
# Сканирование сети 192.168.1.0/24
nmap -sn 192.168.1.0/24

# Сканирование сети 192.168.16.0/24 (как в примере)
nmap -sn 192.168.16.0/24

# Поиск устройств с SSH
nmap -p 22 192.168.16.0/24
```

**Способ 2: Поиск по имени хоста**
```bash
# Поиск по имени grandmini
nslookup grandmini
ping grandmini

# Или поиск по .local домену
ping grandmini.local
```

**Способ 3: Поиск через ARP таблицу**
```bash
# Просмотр ARP таблицы
arp -a

# Поиск активных устройств
ip neigh show
```

#### Подключение по SSH

```bash
# Подключение к найденному устройству
ssh shiwa@192.168.16.163  # или найденный IP
# Пароль: 278934
```

При подключении вы увидите приветственный экран с логотипом **SHIWA TIME** и информацией о **QUANTUM MINI-PCI TIMECARD**:

```
╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║        ███████╗██╗  ██╗██╗██╗    ██╗ █████╗              ║
║        ██╔════╝██║  ██║██║██║    ██║██╔══██╗             ║
║        ███████╗███████║██║██║ █╗ ██║███████║             ║
║        ╚════██║██╔══██║██║██║███╗██║██╔══██║             ║
║        ███████║██║  ██║██║╚███╔███╔╝██║  ██║             ║
║        ╚══════╝╚═╝  ╚═╝╚═╝ ╚══╝╚══╝ ╚═╝  ╚═╝             ║
║        ████████╗██╗███╗   ███╗███████╗                   ║
║        ╚══██╔══╝██║████╗ ████║██╔════╝                   ║
║           ██║   ██║██╔████╔██║█████╗                     ║
║           ██║   ██║██║╚██╔╝██║██╔══╝                     ║
║           ██║   ██║██║ ╚═╝ ██║███████╗                   ║
║           ╚═╝   ╚═╝╚═╝     ╚═╝╚══════╝                   ║
║                                                           ║
║              QUANTUM MINI-PCI TIMECARD                    ║
║                                                           ║
║         Precision Time Protocol Grandmaster               ║
║            GPS/GLONASS Synchronized Clock                 ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════
  Система: Linux 6.6.31+rpt-rpi-v8 aarch64
  Uptime:  2 hours 15 minutes
  IP:     192.168.16.163
  Shiwa Time: ● Running
  GNSS:   ● Connected
═══════════════════════════════════════════════════════════
```

### 2. Быстрые команды

После входа доступны короткие команды:
- `status` - Статус Shiwa Time
- `logs` - Просмотр логов в реальном времени
- `config` - Редактировать конфигурацию
- `restart` - Перезапустить сервис

### 3. Основные операции

**Проверить статус:**
```bash
status
```

**Посмотреть логи:**
```bash
logs
```

**Изменить конфигурацию:**
```bash
config
```

**Перезапустить сервис:**
```bash
restart
```

---

## 📊 Веб-мониторинг

**Grafana Dashboard:** http://90.156.231.174  
**Админ:** `admin` / `admin` | **Пользователь:** `user` / `user`

⚠️ **Важно:** Это тестовый сервер Shiwa Network для мониторинга и диагностики изделий TimeCard Mini. Все метрики производительности и синхронизации времени автоматически выгружаются на этот сервер **при наличии подключения к интернету** для анализа.

**Выгрузку данных можно отключить в конфигурации Shiwa Time.**

Отслеживайте состояние устройства **TESTER** в реальном времени через удобный веб-интерфейс.

---

## 📋 Текущая конфигурация устройства

- **Имя устройства:** TESTER
- **IP-адрес:** 192.168.16.238
- **Пользователь:** shiwa
- **Пароль:** 278934

### Активные источники времени

**Primary Clocks:**
1. **PTP Grandmaster** - Раздача времени по сети
   - Домен: 0
   - Unicast + Multicast
   - Интерфейс: eth0

2. **PPS** - Pulse Per Second
   - Источник: GNSS
   - Интерфейс: eth0
   - Связан с: `/dev/ttyS0`

**Secondary Clocks:**
1. **Timecard Mini** - GPS/GLONASS приемник
   - Устройство: `/dev/ttyS0`
   - GNSS: GPS + ГЛОНАСС
   - Скорость: 115200 baud

### Clock Quality (PTP)
- **Class:** 6 (Primary Reference - GNSS)
- **Accuracy:** 0x20 (25 наносекунд)
- **Time Source:** 0x20 (GPS/GNSS)

### Вывод данных
- **Elasticsearch:** 90.156.231.174:9200 (тестовый сервер Shiwa)
- **Grafana:** http://90.156.231.174 (см. раздел [Веб-мониторинг](#-веб-мониторинг) выше)

---

## 🔍 Проверка работоспособности

### Статус сервиса
```bash
sudo systemctl status shiwatime
```

### PTP порты
```bash
sudo ss -ulnp | grep -E "319|320"
```

### PTP трафик
```bash
sudo tcpdump -i eth0 -nn port 319 or port 320 -c 10
```

### Логи в реальном времени
```bash
sudo journalctl -u shiwatime -f
```

---

## 🖥️ GUI и удаленный доступ

### Удаленный доступ к графическому интерфейсу

**Для запуска ShiwaDiffPHC на сервере мониторинга:**
```bash
# Через X11 Forwarding (рекомендуется)
ssh -X root@90.156.231.174
sudo -E shiwadiffphc
```

**Для доступа к GUI Raspberry Pi:**
```bash
# Включить VNC на Raspberry Pi
ssh shiwa@192.168.16.238
sudo raspi-config
# Interface Options → VNC → Yes

# Подключиться через VNC Viewer к 192.168.16.238:5900
```

**Подробнее:** [GUI_CLI_SWITCH_GUIDE.md](GUI_CLI_SWITCH_GUIDE.md)

---

## 🔒 Безопасность

### Смена паролей по умолчанию

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Смените все пароли по умолчанию перед использованием в продакшене!

```bash
# Подключиться к устройству
ssh shiwa@192.168.16.238
# Пароль: 278934

# Сменить пароль пользователя shiwa
passwd

# Сменить пароль root (если нужен)
sudo passwd root

# Сменить пароль CLI интерфейса Shiwa Time
sudo nano /etc/shiwatime/shiwatime.yml
# Изменить: password: "ваш_надежный_пароль"
```

### Настройка SSH ключей

```bash
# На вашем компьютере создать ключ
ssh-keygen -t ed25519 -C "timecard-mini"

# Скопировать ключ на устройство
ssh-copy-id shiwa@192.168.16.238

# Отключить вход по паролю (опционально)
sudo nano /etc/ssh/sshd_config
# PasswordAuthentication no
sudo systemctl restart ssh
```

### Настройка файрвола

```bash
# Установить UFW
sudo apt install ufw -y

# Разрешить SSH
sudo ufw allow 22/tcp

# Разрешить PTP порты
sudo ufw allow 319/udp
sudo ufw allow 320/udp

# Включить файрвол
sudo ufw enable

# Проверить статус
sudo ufw status
```

---

## 🔄 Клонирование и портирование системы

### Создание образа системы для клонирования

Для создания полного образа настроенной системы TimeCard Mini:

#### Метод 1: Создание образа SD-карты (рекомендуется)

**На Linux/Mac:**
```bash
# Подключить SD-карту к компьютеру
# Определить устройство SD-карты
lsblk  # или diskutil list на Mac

# Создать образ (замените /dev/sdX на ваше устройство)
sudo dd if=/dev/sdX of=quantum-mini-pci-backup.img bs=4M status=progress

# Сжать образ для экономии места
gzip quantum-mini-pci-backup.img
```

**На Windows:**
```bash
# Использовать Win32DiskImager или аналогичную программу
# Создать образ .img файла
```

#### Метод 2: Создание архива конфигурации

```bash
# Подключиться к устройству
ssh shiwa@192.168.16.163

# Создать архив всех настроек
sudo tar -czf /tmp/quantum-mini-config.tar.gz \
  /etc/shiwatime/ \
  /etc/systemd/system/shiwatime.service \
  /etc/profile.d/quantum-boot.sh \
  /home/shiwa/.bashrc \
  /boot/config.txt \
  /etc/ssh/sshd_config

# Скачать архив
scp shiwa@192.168.16.163:/tmp/quantum-mini-config.tar.gz ./
```

### Восстановление из образа

#### Восстановление полного образа SD-карты

```bash
# Распаковать образ (если сжат)
gunzip quantum-mini-pci-backup.img.gz

# Записать образ на SD-карту
sudo dd if=quantum-mini-pci-backup.img of=/dev/sdX bs=4M status=progress

# Синхронизировать данные
sync
```

#### Восстановление конфигурации

```bash
# Подключиться к новому устройству
ssh shiwa@192.168.16.163

# Загрузить архив конфигурации
scp quantum-mini-config.tar.gz shiwa@192.168.16.163:/tmp/

# Распаковать конфигурацию
sudo tar -xzf /tmp/quantum-mini-config.tar.gz -C /

# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить и запустить сервисы
sudo systemctl enable shiwatime
sudo systemctl start shiwatime
```

### Настройка после клонирования

После восстановления образа необходимо:

1. **Изменить сетевые настройки:**
```bash
# Изменить hostname
sudo hostnamectl set-hostname new-device-name

# Настроить статический IP (если нужно)
sudo nano /etc/dhcpcd.conf
```

2. **Обновить SSH ключи:**
```bash
# Удалить старые ключи хоста
sudo rm /etc/ssh/ssh_host_*

# Сгенерировать новые ключи
sudo dpkg-reconfigure openssh-server
```

3. **Изменить пароли:**
```bash
# Сменить пароль пользователя
passwd

# Сменить пароль root
sudo passwd root
```

4. **Проверить работу сервисов:**
```bash
# Проверить статус Shiwa Time
status

# Проверить логи
logs
```

### Автоматический скрипт клонирования

Создайте скрипт для автоматического создания образа:

```bash
#!/bin/bash
# create-quantum-backup.sh

DEVICE="/dev/sdX"  # Замените на ваше устройство
BACKUP_NAME="quantum-mini-pci-$(date +%Y%m%d-%H%M%S)"

echo "Создание образа Quantum Mini-PCI..."
sudo dd if=$DEVICE of=${BACKUP_NAME}.img bs=4M status=progress

echo "Сжатие образа..."
gzip ${BACKUP_NAME}.img

echo "Создание контрольной суммы..."
sha256sum ${BACKUP_NAME}.img.gz > ${BACKUP_NAME}.img.gz.sha256

echo "Готово! Образ: ${BACKUP_NAME}.img.gz"
```

---

## 📦 Резервное копирование

### Автоматический бэкап конфигурации

```bash
# Создать скрипт бэкапа
sudo nano /usr/local/bin/backup-shiwatime.sh
```

Содержимое скрипта:
```bash
#!/bin/bash
BACKUP_DIR="/home/shiwa/backups"
DATE=$(date +%Y%m%d_%H%M%S)

mkdir -p $BACKUP_DIR

# Бэкап конфигурации
sudo cp /etc/shiwatime/shiwatime.yml $BACKUP_DIR/shiwatime_$DATE.yml

# Бэкап systemd сервиса
sudo cp /lib/systemd/system/shiwatime.service $BACKUP_DIR/shiwatime.service_$DATE

# Создать архив
cd $BACKUP_DIR
tar -czf shiwatime_backup_$DATE.tar.gz shiwatime_$DATE.yml shiwatime.service_$DATE

# Удалить старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "shiwatime_backup_*.tar.gz" -mtime +30 -delete

echo "Бэкап создан: shiwatime_backup_$DATE.tar.gz"
```

```bash
# Сделать исполняемым
sudo chmod +x /usr/local/bin/backup-shiwatime.sh

# Добавить в cron (ежедневно в 2:00)
sudo crontab -e
# 0 2 * * * /usr/local/bin/backup-shiwatime.sh
```

### Восстановление из бэкапа

```bash
# Распаковать бэкап
tar -xzf shiwatime_backup_YYYYMMDD_HHMMSS.tar.gz

# Восстановить конфигурацию
sudo cp shiwatime_YYYYMMDD_HHMMSS.yml /etc/shiwatime/shiwatime.yml

# Перезапустить сервис
sudo systemctl restart shiwatime
```

---

## ⚡ Оптимизация производительности

### Настройки для максимальной точности

```bash
# Отключить ненужные сервисы
sudo systemctl disable bluetooth
sudo systemctl disable wifi-powersave
sudo systemctl disable avahi-daemon

# Оптимизировать сетевые настройки
sudo nano /etc/sysctl.conf
```

Добавить в `/etc/sysctl.conf`:
```ini
# Оптимизация сети для PTP
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728
net.ipv4.udp_mem = 102400 873800 16777216
net.ipv4.udp_rmem_min = 8192
net.ipv4.udp_wmem_min = 8192
```

```bash
# Применить настройки
sudo sysctl -p
```

### Мониторинг ресурсов

```bash
# Установить htop для мониторинга
sudo apt install htop -y

# Мониторинг в реальном времени
htop

# Проверка использования диска
df -h

# Проверка памяти
free -h
```

---

## 🔄 Обновления системы

### Обновление Raspberry Pi OS

```bash
# Обновить список пакетов
sudo apt update

# Обновить установленные пакеты
sudo apt upgrade -y

# Очистить кэш
sudo apt autoremove -y
sudo apt autoclean
```

### Обновление Shiwa Time

```bash
# Проверить текущую версию
shiwatime version

# Обновить Shiwa Time (если доступно)
sudo apt update
sudo apt upgrade shiwatime -y

# Перезапустить сервис
sudo systemctl restart shiwatime
```

### Проверка совместимости

```bash
# Проверить версию ядра
uname -r

# Проверить версию Raspberry Pi OS
cat /etc/os-release

# Проверить архитектуру
uname -m
```

---

## ❓ Часто задаваемые вопросы (FAQ)

### Q: Как проверить, что PTP работает?

```bash
# Проверить статус сервиса
sudo systemctl status shiwatime

# Проверить PTP порты
sudo ss -ulnp | grep -E "319|320"

# Захватить PTP трафик
sudo tcpdump -i eth0 -nn port 319 or port 320 -c 5
```

### Q: Почему не работает GNSS?

1. Проверьте антенну (должна видеть небо)
2. Проверьте кабель (не поврежден)
3. Проверьте питание антенны
4. Подождите 5-10 минут для захвата спутников

```bash
# Проверить GNSS устройство
ls -l /dev/ttyS0

# Проверить логи GNSS
sudo journalctl -u shiwatime | grep -i gnss
```

### Q: Как изменить IP адрес?

```bash
# Через NetworkManager
sudo nmcli connection show
sudo nmcli connection modify "Ethernet connection 1" \
  ipv4.addresses 192.168.1.100/24 \
  ipv4.gateway 192.168.1.1 \
  ipv4.method manual
sudo nmcli connection up "Ethernet connection 1"
```

### Q: Данные не отображаются в Grafana

1. Проверьте интернет подключение
2. Проверьте конфигурацию Elasticsearch
3. Проверьте логи сервиса

```bash
# Проверить отправку данных
sudo journalctl -u shiwatime -f | grep elasticsearch

# Проверить доступность сервера
curl http://90.156.231.174:9200
```

### Q: Как отключить выгрузку данных?

```bash
# Отредактировать конфигурацию
sudo nano /etc/shiwatime/shiwatime.yml

# Закомментировать секцию:
# output.elasticsearch:
#   hosts: ['90.156.231.174:9200']

# Перезапустить сервис
sudo systemctl restart shiwatime
```

---

## 🎯 Примеры использования

### Сценарий 1: PTP Grandmaster для локальной сети

**Назначение:** Раздача точного времени по PTP для синхронизации устройств в локальной сети.

**Конфигурация:**
```yaml
timebeat:
  clock_sync:
    primary_clocks:
      - protocol: ptp
        domain: 0
        serve_unicast: true
        serve_multicast: true
        server_only: true
        interface: eth0
      
      - protocol: pps
        interface: eth0
        pin: 0
        edge_mode: "rising"
        linked_device: '/dev/ttyS0'
    
    secondary_clocks:
      - protocol: timebeat_opentimecard_mini
        device: '/dev/ttyS0'
        baud: 115200
        card_config: ['gnss1:signal:gps+glonass']
```

**Применение:** Синхронизация серверов, рабочих станций, сетевого оборудования.

### Сценарий 2: Мониторинг без синхронизации

**Назначение:** Только мониторинг качества синхронизации без изменения системного времени.

**Конфигурация:**
```yaml
timebeat:
  clock_sync:
    adjust_clock: false  # Только мониторинг!
    
    primary_clocks:
      - protocol: ptp
        domain: 0
        interface: eth0
        monitor_only: true
```

**Применение:** Тестирование, анализ качества сети, диагностика.

### Сценарий 3: Резервирование с NTP

**Назначение:** PTP как основной источник, NTP как резервный.

**Конфигурация:**
```yaml
timebeat:
  clock_sync:
    primary_clocks:
      - protocol: ptp
        domain: 0
        interface: eth0
        monitor_only: false
    
    secondary_clocks:
      - protocol: ntp
        ip: 'pool.ntp.org'
        pollinterval: 4s
```

**Применение:** Высокая надежность в критических системах.

### Сценарий 4: Изолированная сеть

**Назначение:** Работа без интернета, только локальная синхронизация.

**Конфигурация:**
```yaml
timebeat:
  clock_sync:
    primary_clocks:
      - protocol: pps
        interface: eth0
        pin: 0
        edge_mode: "rising"
        linked_device: '/dev/ttyS0'
    
    secondary_clocks:
      - protocol: timebeat_opentimecard_mini
        device: '/dev/ttyS0'
        baud: 115200
        card_config: ['gnss1:signal:gps+glonass']

# Отключить выгрузку данных
# output.elasticsearch: # закомментировано
```

**Применение:** Защищенные сети, военные объекты, критическая инфраструктура.

---

## 📊 Сравнение режимов работы

| Режим | Точность | Надежность | Сложность | Применение |
|-------|----------|------------|-----------|------------|
| **PTP Grandmaster** | ±25 ns | Высокая | Средняя | Промышленность, телеком |
| **PTP Client** | ±100 ns | Средняя | Низкая | Офисы, лаборатории |
| **NTP** | ±1 ms | Высокая | Низкая | Общие задачи |
| **Мониторинг** | N/A | N/A | Низкая | Тестирование, анализ |

---

## 🔧 Диагностические команды

### Быстрая проверка системы

```bash
# Создать скрипт диагностики
sudo nano /usr/local/bin/diagnose-shiwa.sh
```

Содержимое:
```bash
#!/bin/bash
echo "=== Диагностика Shiwa Time ==="
echo "Дата: $(date)"
echo

echo "1. Статус сервиса:"
systemctl status shiwatime --no-pager -l

echo -e "\n2. PTP порты:"
ss -ulnp | grep -E "319|320"

echo -e "\n3. GNSS устройство:"
ls -l /dev/ttyS0

echo -e "\n4. Сетевые интерфейсы:"
ip addr show eth0

echo -e "\n5. Последние логи:"
journalctl -u shiwatime -n 10 --no-pager

echo -e "\n6. Использование ресурсов:"
free -h
df -h /

echo -e "\n7. Температура (если доступно):"
vcgencmd measure_temp 2>/dev/null || echo "Температура недоступна"
```

```bash
# Сделать исполняемым
sudo chmod +x /usr/local/bin/diagnose-shiwa.sh

# Запустить диагностику
sudo /usr/local/bin/diagnose-shiwa.sh
```

### Мониторинг в реальном времени

```bash
# Создать скрипт мониторинга
sudo nano /usr/local/bin/monitor-shiwa.sh
```

Содержимое:
```bash
#!/bin/bash
while true; do
    clear
    echo "=== Мониторинг Shiwa Time ==="
    echo "Время: $(date)"
    echo
    
    # Статус сервиса
    if systemctl is-active --quiet shiwatime; then
        echo "✅ Shiwa Time: Активен"
    else
        echo "❌ Shiwa Time: Остановлен"
    fi
    
    # PTP порты
    PTP_PORTS=$(ss -ulnp | grep -E "319|320" | wc -l)
    echo "📡 PTP порты: $PTP_PORTS открыто"
    
    # GNSS
    if [ -c /dev/ttyS0 ]; then
        echo "🛰️ GNSS: Подключен"
    else
        echo "❌ GNSS: Не найден"
    fi
    
    # Загрузка CPU
    CPU_USAGE=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)
    echo "💻 CPU: ${CPU_USAGE}%"
    
    # Память
    MEM_USAGE=$(free | grep Mem | awk '{printf("%.1f%%", $3/$2 * 100.0)}')
    echo "🧠 Память: $MEM_USAGE"
    
    echo -e "\nНажмите Ctrl+C для выхода"
    sleep 5
done
```

```bash
# Сделать исполняемым
sudo chmod +x /usr/local/bin/monitor-shiwa.sh

# Запустить мониторинг
sudo /usr/local/bin/monitor-shiwa.sh
```

---

## 📞 Поддержка

- **Документация Shiwa Network:** https://shiwanetwork.tech/
- **Основное руководство:** [SHIWATIME_GUIDE.md](SHIWATIME_GUIDE.md)
- **Подробная конфигурация:** [shiwatime_config_guide.md](shiwatime_config_guide.md)
- **GUI/CLI переключение:** [GUI_CLI_SWITCH_GUIDE.md](GUI_CLI_SWITCH_GUIDE.md)
- **Мониторинг:** [MONITORING.md](MONITORING.md)

---

## 📄 Лицензия и авторские права

Данная документация создана для Quantum Mini-PCI Timecard на базе Shiwa Time.

**Версия документации:** 1.0  
**Дата обновления:** $(date +%Y-%m-%d)  
**Статус:** Готова к использованию

---

*Для получения технической поддержки обращайтесь к разработчикам Shiwa Network.*
