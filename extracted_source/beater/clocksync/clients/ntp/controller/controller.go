package controller

// Автоматически извлечено из timebeat-2.2.20
// Реализация контроллера NTP находится в родительском пакете: beater/clocksync/clients/ntp (client_impl.go).
// NewController, Start, loadConfig, ConfigureTimeSource, configureAndStartClient/Server — в ntp package.

func Extracted_Go() {
	// Реализация в ntp.NewController, ntp.(*Controller).Start и т.д. (client_impl.go).
}

