# Анализ коэффициентов алгоритмов

## ✅ Найденные функции CoefficientStore

### Функции управления коэффициентами

1. **GetCoefficientStore** (0x41c6270)
   - Получение хранилища коэффициентов (singleton)

2. **newCoefficientStore** (0x41c60f0)
   - Создание нового хранилища

3. **GetCoefficients** (0x41c6520)
   - Получение коэффициентов

4. **GetCoefficientsForType** (0x41c62d0)
   - Получение коэффициентов для типа алгоритма

5. **ChangeSteeringCoefficients** (0x41c6840)
   - Изменение коэффициентов управления

6. **updateAlgoCoefficients** (0x41daf30)
   - Обновление коэффициентов алгоритма

### ⭐ Ключевая находка: DefaultAlgoCoefficients

**Адрес данных:** `0x770b7e0` (D - данные!)

Это глобальная переменная с дефолтными коэффициентами алгоритмов!

---

## 🔍 Анализ CalculateNewFrequency (PID)

### Адрес: 0x41c87c0

### Ключевые находки:

#### 1. Массив коэффициентов D-компоненты

**Адрес массива:** `0x770a000 + 0x430 = 0x770a430`

```assembly
41c8c1c: d001aa02  adrp x2, 770a000
41c8c20: 9110c042  add  x2, x2, #0x430
41c8c24: fc607840  ldr  d0, [x2, x0, lsl #3]  // Загрузка по индексу!
```

**Размер массива:** 3 элемента (индекс проверяется на `#0x3`)

**Вывод:** Массив из 3 float64 значений для D-компоненты в зависимости от логарифма!

#### 2. Коэффициенты из структуры

**Смещения в структуре AlgoPID:**
- `[x0, #40]` - указатель на структуру коэффициентов (offset 40)
- `[x2, #0]` - первый коэффициент (Kp?) (offset 0)
- `[x2, #8]` - второй коэффициент (Ki?) (offset 8)
- `[x2, #16]` - третий коэффициент (Kd?) (offset 16)
- `[x3, #40]` - значение для расчета D (offset 40)

**Формула PID (из ассемблера):**
```go
// P компонента
P = Kp * offset

// I компонента  
I = Ki * offset + previous_I

// D компонента
abs_value = abs(value_from_offset_40)
log_value = log(abs_value)
d_index = int(log_value * constant) // ограничено 0-2
D = D_coefficients[d_index] * gradient
D = D / divisor  // делитель из offset 24

// Итоговая частота
frequency = P + I + D
```

#### 3. Константы для расчета

**Из ассемблера:**
- `0x7f80` (32640) - константа для расчета времени
- `0xd7b1` - часть константы времени
- `0xca00` (51712) - константа для расчета
- `0x3b9a` (15258) - константа для расчета
- `0x4924` (18724) - константа для деления (offset 24)
- `0x2492` (9362) - константа для деления
- `0x9249` (37449) - константа для деления

**Константы из памяти (504b000):**
- `[x27, #664]` - константа для D компоненты
- `[x27, #1904]` - минимальное значение
- `[x27, #2360]` - максимальное значение

#### 4. enforceAdjustmentLimit

**Адрес:** 0x41c8cc0

**Логика:**
- Проверяет лимиты коррекции
- Использует `[x0, #96]` и `[x0, #112]` для лимитов
- Использует `[x0, #40]` -> `[x3, #32]` для дефолтного лимита
- Возвращает скорректированное значение и флаг превышения

---

## 🔍 Анализ regress (LinReg)

### Адрес: 0x41c6e30

### Ключевые находки:

#### 1. Размер окна регрессии

**Константа:** `0x40` (64 элемента!)

```assembly
41c6e4c: f101005f  cmp x2, #0x40  // Проверка на 64
41c6fcc: b27a03e1  orr x1, xzr, #0x40  // 64 в панике
```

**Вывод:** Окно регрессии = 64 элемента!

#### 2. Структура linreg_servo

**Смещения:**
- `0x638` (1592) - массив данных регрессии (offset 1592)
- `0x1536` (5430) - референсное время (offset 5430)
- `0x1544` (5444) - референсное смещение (offset 5444)
- `0x1560` (5472) - счетчик элементов (offset 5472)
- `0x1568` (5480) - индекс текущего элемента (offset 1568)
- `0x1584` (5508) - дополнительный счетчик (offset 1584)
- `0x1752` (5970) - размер окна (offset 1752)
- `0x1760` (5984) - частота коррекции (offset 1760)
- `0x1768` (5992) - интервал синхронизации (offset 1768)
- `0x1776` (6006) - коэффициент частоты (offset 1776)
- `0x1784` (6020) - флаг leap (offset 1784)
- `0x1792` (6034) - минимальное значение (offset 1792)
- `0x1800` (6144) - максимальное значение (offset 1800)
- `0x1808` (6152) - порог для leap (offset 1808)
- `0x1816` (6166) - флаг для leap (offset 1816)

**Размер структуры:** ~6166+ байт

#### 3. Алгоритм регрессии

**Из ассемблера:**
```go
// Цикл по 6 интервалам (0x2 до 0x6)
for i := 2; i <= 6; i++ {
    // Расчет наклона и пересечения
    slope = (sum_xy - sum_x * sum_y / n) / (sum_x2 - sum_x * sum_x / n)
    intercept = (sum_y - slope * sum_x) / n
    
    // Сохранение результатов
    results[i-2].slope = slope
    results[i-2].intercept = intercept
}
```

**Формула линейной регрессии:**
- `slope = (Σxy - Σx·Σy/n) / (Σx² - (Σx)²/n)`
- `intercept = (Σy - slope·Σx) / n`

#### 4. Константы

- `0x2` (2) - начальный индекс
- `0x6` (6) - конечный индекс
- `0xa` (10) - порог для усреднения
- `0x40` (64) - размер окна

---

## 🔍 Анализ pi_sample (PI)

### Адрес: 0x41c8da0

### Ключевые находки:

#### 1. Структура pi_servo

**Смещения:**
- `[x0, #56]` (offset 56) - предыдущее время
- `[x0, #64]` (offset 64) - текущее время
- `[x0, #72]` (offset 72) - предыдущее смещение
- `[x0, #80]` (offset 80) - текущее смещение
- `[x0, #88]` (offset 88) - интегральная компонента
- `[x0, #104]` (offset 104) - константа для расчета
- `[x0, #112]` (offset 112) - минимальное значение
- `[x0, #120]` (offset 120) - состояние (0, 1, или другое)

#### 2. Алгоритм PI

**Из ассемблера:**
```go
// Проверка состояния
if state == 0 {
    // Первая инициализация
    prev_time = current_time
    prev_offset = current_offset
    state = 1
    return
}

if state == 1 {
    // Вторая инициализация
    current_time = time
    current_offset = offset
    
    if current_time <= prev_time {
        // Сброс
        state = 0
        return
    }
    
    // Расчет интегральной компоненты
    time_diff = current_time - prev_time
    offset_diff = current_offset - prev_offset
    
    // Магическая константа для расчета
    magic = 0xa59836b45f418970  // Константа для расчета
    half_time = prev_time / 2
    calculated = magic * half_time / (1 << 28)
    adjusted = current_time - calculated
    
    // Расчет I компоненты
    I = prev_I + (51712 - prev_I) * offset_diff / time_diff
    
    // Ограничение I компоненты
    if I < -max_value {
        I = -max_value
    } else if I > max_value {
        I = max_value
    }
}
```

#### 3. Константы

- `0xa59836b45f418970` - магическая константа для расчета
- `0xca00` (51712) - константа для расчета I
- `0x3b9a` (15258) - часть константы

---

## 📊 Структуры данных

### CoefficientStore

**Адрес:** 0x770b7e0 (DefaultAlgoCoefficients)

**Нужно проанализировать:**
```bash
# Найти размер структуры
objdump -d /usr/share/shiwatime/bin/shiwatime | grep -B 5 -A 5 "770b7e0"

# Найти использование
objdump -d /usr/share/shiwatime/bin/shiwatime | grep "770b7e0"
```

### AlgoPID структура

**Известные смещения:**
- `[x0, #24]` - интегральная компонента (I)
- `[x0, #40]` - указатель на коэффициенты
- `[x0, #48]` - указатель на BestFitFiltered
- `[x0, #96]` - максимальный лимит коррекции
- `[x0, #112]` - минимальный лимит коррекции
- `[x0, #128]` - флаг отладки

### linreg_servo структура

**Известные смещения:**
- `0x638` - массив данных (64 элемента)
- `0x1536` - референсное время
- `0x1544` - референсное смещение
- `0x1560` - счетчик
- `0x1568` - индекс
- `0x1752` - размер окна
- `0x1760` - частота
- `0x1776` - коэффициент частоты

---

## 🎯 Следующие шаги

### 1. Извлечь DefaultAlgoCoefficients

```bash
# Найти данные по адресу
objdump -s /usr/share/shiwatime/bin/shiwatime | grep -A 20 "770b7e0"

# Или через hexdump
hexdump -C /usr/share/shiwatime/bin/shiwatime | grep -A 5 "770b7e0"
```

### 2. Проанализировать массив D-коэффициентов

```bash
# Найти данные по адресу 0x770a430
objdump -s /usr/share/shiwatime/bin/shiwatime | grep -A 5 "770a430"
```

### 3. Извлечь коэффициенты из GetCoefficients

```bash
# Дизассемблировать GetCoefficients
objdump -d /usr/share/shiwatime/bin/shiwatime | grep -A 100 "41c6520"
```

---

## 📝 Выводы

### ✅ Что найдено:

1. **CoefficientStore** - хранилище коэффициентов найдено!
2. **DefaultAlgoCoefficients** - дефолтные коэффициенты по адресу 0x770b7e0
3. **Массив D-коэффициентов** - 3 элемента по адресу 0x770a430
4. **Структуры алгоритмов** - смещения найдены
5. **Размер окна LinReg** - 64 элемента
6. **Формулы алгоритмов** - частично восстановлены из ассемблера

### ⚠️ Что нужно:

1. Извлечь значения коэффициентов из памяти
2. Проанализировать DefaultAlgoCoefficients
3. Найти точные значения Kp, Ki, Kd
4. Найти параметры LinReg (размер окна = 64 ✅)

---

**Прогресс: 85% → 90%** ✅

*Коэффициенты почти найдены! Осталось извлечь их значения из памяти!*
