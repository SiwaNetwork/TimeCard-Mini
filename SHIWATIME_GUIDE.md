# Руководство по работе с Shiwa Time (Timecard Mini)

## ⚠️ Важное примечание

На устройстве может быть установлено программное обеспечение как **Timebeat**, так и **Shiwa Network** (Shiwa Time).

**Данная документация описывает работу только с Shiwa Time.**

---

## 📡 Подключение к устройству по SSH

### Способ 1: Подключение с использованием пароля

```bash
ssh shiwa@192.168.16.238
```

После выполнения команды система запросит пароль. Введите пароль: `278934`

### Способ 2: Подключение с использованием SSH-ключа (рекомендуется)

Для более безопасного и удобного подключения настройте SSH-ключи:

1. Создайте SSH-ключ на вашем компьютере (если еще не создан):
```bash
ssh-keygen -t ed25519 -C "shiwa@timecard-mini"
```

2. Скопируйте публичный ключ на Timecard Mini:
```bash
ssh-copy-id shiwa@192.168.16.238
```

3. Теперь можно подключаться без пароля:
```bash
ssh shiwa@192.168.16.238
```

### Проверка IP-адреса устройства

Если IP-адрес устройства изменился, найдите его в сети:

```bash
# Сканирование локальной сети
nmap -sn 192.168.16.0/24

# Или используйте arp
arp -a | grep raspberrypi
```

### Приветственный экран при подключении

При успешном подключении к устройству вы увидите кастомный логотип **SHIWA TIME** и информацию о **QUANTUM MINI-PCI TIMECARD**:

```
╔═══════════════════════════════════════════════════════════╗
║                                                           ║
║        ███████╗██╗  ██╗██╗██╗    ██╗ █████╗              ║
║        ██╔════╝██║  ██║██║██║    ██║██╔══██╗             ║
║        ███████╗███████║██║██║ █╗ ██║███████║             ║
║        ╚════██║██╔══██║██║██║███╗██║██╔══██║             ║
║        ███████║██║  ██║██║╚███╔███╔╝██║  ██║             ║
║        ╚══════╝╚═╝  ╚═╝╚═╝ ╚══╝╚══╝ ╚═╝  ╚═╝             ║
║        ████████╗██╗███╗   ███╗███████╗                   ║
║        ╚══██╔══╝██║████╗ ████║██╔════╝                   ║
║           ██║   ██║██╔████╔██║█████╗                     ║
║           ██║   ██║██║╚██╔╝██║██╔══╝                     ║
║           ██║   ██║██║ ╚═╝ ██║███████╗                   ║
║           ╚═╝   ╚═╝╚═╝     ╚═╝╚══════╝                   ║
║                                                           ║
║              QUANTUM MINI-PCI TIMECARD                    ║
║                                                           ║
║         Precision Time Protocol Grandmaster               ║
║            GPS/GLONASS Synchronized Clock                 ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════
  Система: Linux 6.6.31+rpt-rpi-v8 aarch64
  Uptime:  2 hours 15 minutes
  IP:     192.168.16.163
  Shiwa Time: ● Running
  GNSS:   ● Connected
═══════════════════════════════════════════════════════════

Быстрые команды:
  status   - Статус Shiwa Time
  logs     - Просмотр логов
  config   - Редактировать конфигурацию
  restart  - Перезапустить сервис
```

Этот экран помогает быстро идентифицировать устройство, показывает текущий статус системы и напоминает о доступных быстрых командах.

**Доступные быстрые команды:**
- `status` - Проверить статус Shiwa Time (`sudo systemctl status shiwatime`)
- `logs` - Просмотр логов в реальном времени (`sudo journalctl -u shiwatime -f`)
- `config` - Редактировать конфигурацию (`sudo nano /etc/shiwatime/shiwatime.yml`)
- `restart` - Перезапустить сервис (`sudo systemctl restart shiwatime`)

**Настройка приветственного экрана:**
Подробная инструкция по изменению логотипа и настройке загрузочного экрана находится в файле `CUSTOM_BOOT_GUIDE.md`.

---

## ⚙️ Работа с сервисом Shiwa Time

### Управление сервисом

**Проверка статуса:**
```bash
sudo systemctl status shiwatime
```

**Запуск сервиса:**
```bash
sudo systemctl start shiwatime
```

**Остановка сервиса:**
```bash
sudo systemctl stop shiwatime
```

**Перезапуск сервиса:**
```bash
sudo systemctl restart shiwatime
```

**Включить автозапуск при загрузке:**
```bash
sudo systemctl enable shiwatime
```

**Отключить автозапуск:**
```bash
sudo systemctl disable shiwatime
```

### Просмотр логов

**Просмотр последних логов:**
```bash
sudo journalctl -u shiwatime -n 50
```

**Просмотр логов в реальном времени:**
```bash
sudo journalctl -u shiwatime -f
```

**Просмотр логов с определенного времени:**
```bash
sudo journalctl -u shiwatime --since "1 hour ago"
sudo journalctl -u shiwatime --since "2025-10-02 08:00:00"
```

---

## 📝 Редактирование конфигурации

### Основной конфигурационный файл

Конфигурация Shiwa Time находится в файле:
```
/etc/shiwatime/shiwatime.yml
```

### Способ 1: Редактирование через nano (простой редактор)

1. Подключитесь к устройству по SSH:
```bash
ssh shiwa@192.168.16.238
```

2. Откройте конфигурационный файл:
```bash
sudo nano /etc/shiwatime/shiwatime.yml
```

3. Внесите необходимые изменения

4. Сохраните файл:
   - Нажмите `Ctrl + O` (сохранить)
   - Нажмите `Enter` (подтвердить имя файла)
   - Нажмите `Ctrl + X` (выйти)

5. Перезапустите сервис для применения изменений:
```bash
sudo systemctl restart shiwatime
```

### Способ 2: Редактирование через vim (продвинутый редактор)

```bash
sudo vim /etc/shiwatime/shiwatime.yml
```

Основные команды vim:
- `i` - войти в режим редактирования
- `Esc` - выйти из режима редактирования
- `:w` - сохранить
- `:q` - выйти
- `:wq` - сохранить и выйти
- `:q!` - выйти без сохранения

### Способ 3: Редактирование с локального компьютера

1. Скачайте файл на локальный компьютер:
```bash
scp shiwa@192.168.16.238:/etc/shiwatime/shiwatime.yml ./shiwatime.yml
```

2. Отредактируйте файл в вашем любимом редакторе

3. Загрузите обратно на устройство:
```bash
scp ./shiwatime.yml shiwa@192.168.16.238:/tmp/shiwatime.yml
ssh shiwa@192.168.16.238 "sudo cp /tmp/shiwatime.yml /etc/shiwatime/shiwatime.yml"
```

4. Перезапустите сервис:
```bash
ssh shiwa@192.168.16.238 "sudo systemctl restart shiwatime"
```

---

## 🔧 Основные параметры конфигурации

### Настройка PPS (Pulse Per Second)

```yaml
primary_clocks:
  - protocol:          pps
    interface:         eth0         # Ethernet интерфейс
    pin:               0            # Номер контакта (SDP)
    index:             0            # Индекс / канал
    cable_delay:       0            # Задержка кабеля в наносекундах (~5 нс на метр)
    edge_mode:         "rising"     # Режим триггера: "rising", "falling" или "both"
    monitor_only:      false        # Только мониторинг или также синхронизация
    atomic:            false        # Атомные часы?
    linked_device:     '/dev/ttyS0' # Связанное устройство NMEA/Timecard
```

### Настройка PTP (Precision Time Protocol)

```yaml
primary_clocks:
  - protocol:                 ptp
    domain:                   0
    serve_unicast:           true
    serve_multicast:         true
    announce_interval:        1
    sync_interval:            0
    delayrequest_interval:    0
    disable:                  false  # false для включения
    interface:               eth0    # Сетевой интерфейс
```

### Настройка NTP

```yaml
primary_clocks:
  - protocol:      ntp
    ip:            'pool.ntp.org'  # NTP сервер
    pollinterval:  4s                # Интервал опроса
    monitor_only:  false
```

### Настройка Timecard Mini

```yaml
secondary_clocks:
  - protocol:     timebeat_opentimecard_mini
    device:       '/dev/ttyS0'      # Последовательный порт
    baud:         115200             # Скорость передачи
    card_config:  ['gnss1:signal:gps+glonass']
    offset:       225000000          # Статическое смещение
    atomic:       false
    monitor_only: false
```

### Настройка вывода данных (Elasticsearch)

```yaml
output.elasticsearch:
  hosts: ['90.156.231.174:9200']  # Адрес Elasticsearch сервера
  # protocol: 'https'              # Протокол (для Timebeat Cloud)
  # username: 'elastic'            # Имя пользователя
  # password: 'changeme'           # Пароль
```

⚠️ **Примечание:** Выгрузка данных работает только при наличии подключения к интернету. Для отключения отправки данных на сервер закомментируйте секцию `output.elasticsearch` или удалите её из конфигурации.

### Настройка имени устройства

```yaml
name: TESTER  # Имя устройства для идентификации в системе мониторинга
```

---

## 🛠️ Дополнительные команды

### Проверка сетевых интерфейсов

```bash
# Список сетевых интерфейсов
ip addr show

# Информация о PTP интерфейсе
ethtool -T eth0
```

### Проверка PTP устройств

```bash
# Список PTP устройств
ls -l /dev/ptp*

# Информация о PTP часах
sudo phc_ctl /dev/ptp0 get
```

### Проверка системного времени

```bash
# Текущее время и статус синхронизации
timedatectl status

# Информация о часовом поясе
timedatectl list-timezones | grep Europe
```

### Мониторинг работы в реальном времени

```bash
# Просмотр логов синхронизации в реальном времени
sudo journalctl -u shiwatime -f | grep "ts,"
```

---

## 📊 Веб-мониторинг через Grafana и Elasticsearch

Все метрики работы устройства автоматически отправляются в Elasticsearch и доступны для просмотра через Grafana.

### Доступ к системе мониторинга

**Адрес Grafana:**
```
http://90.156.231.174
```

⚠️ **Важно:** Это тестовый сервер Shiwa Network для мониторинга и диагностики изделий TimeCard Mini. На этот сервер автоматически выгружаются все метрики работы вашего устройства **при наличии подключения к интернету**:
- Данные синхронизации времени (offset, jitter, drift)
- PTP метрики (Sync messages, Delay Request/Response)
- GNSS статус (количество спутников, качество сигнала)
- Системные показатели производительности

Данные используются для анализа работы устройства и диагностики проблем.

**Выгрузку данных можно отключить в конфигурации Shiwa Time.**

**Учетные данные:**
- **Админ:** `admin` / `admin` (полный доступ к настройкам)
- **Пользователь:** `user` / `user` (просмотр дашбордов)

### Что можно мониторить

Система собирает и отображает следующие данные об устройстве **TESTER**:

#### 🕐 Временная синхронизация
- **Offset (смещение времени)** - отклонение от источника времени в наносекундах
- **Drift (дрейф)** - скорость изменения смещения
- **Jitter (джиттер)** - вариация задержки сигнала
- **Frequency adjustment** - коррекция частоты системных часов

#### 📡 PTP метрики
- **PTP Sync сообщения** - частота и качество синхронизации
- **Delay Request/Response** - задержки в сети
- **Path delay** - задержка распространения сигнала
- **Clock quality** - качество часов (class, accuracy, variance)

#### 🛰️ GNSS метрики
- **Количество спутников** - видимые GPS/GLONASS спутники
- **Качество сигнала** - SNR (signal-to-noise ratio)
- **Статус фиксации** - 2D/3D fix
- **GNSS смещение** - отклонение GNSS от системного времени

#### 🌐 Сетевые метрики
- **Packet rate** - количество PTP пакетов в секунду
- **Network jitter** - сетевой джиттер
- **Unicast/Multicast статистика**

#### ⚙️ Системные метрики
- **Uptime** - время работы устройства
- **CPU и память** (если настроено)
- **Температура** (если доступно)

### Как использовать Grafana

**1. Войдите в систему:**
   - Откройте браузер
   - Перейдите на http://90.156.231.174
   - Введите логин `user` и пароль `user`

**2. Найдите дашборды:**
   - В левом меню выберите **Dashboards** (иконка с четырьмя квадратами)
   - Найдите дашборд для устройства **TESTER**
   - Или используйте поиск по имени устройства

**3. Настройте временной диапазон:**
   - В правом верхнем углу выберите период отображения:
     - Last 5 minutes - последние 5 минут
     - Last 1 hour - последний час
     - Last 24 hours - последние сутки
     - Last 7 days - последняя неделя
     - Custom range - произвольный период

**4. Автообновление:**
   - Включите автообновление (refresh) в правом верхнем углу
   - Рекомендуется: 5s или 10s для реального времени

### Типичные дашборды

#### Time Synchronization Overview
Общий обзор синхронизации времени:
- График offset в реальном времени
- Текущее качество синхронизации
- Статус источников времени (PPS, GNSS)
- Алерты при превышении пороговых значений

#### PTP Performance
Производительность PTP:
- Delay Request/Response графики
- Mean Path Delay
- PTP packet rate
- Master clock information

#### GNSS Status
Состояние GNSS приемника:
- Количество спутников
- Качество сигнала (SNR)
- Координаты (если включено)
- Статус антенны

### Настройка алертов

Grafana может отправлять уведомления при:
- Превышении offset больше порогового значения (например, >100ns)
- Потере GNSS сигнала
- Потере PTP синхронизации
- Критических ошибках в логах

**Настройка алертов** (если есть права администратора):
1. Откройте нужный график
2. Нажмите на название панели → Edit
3. Перейдите на вкладку Alert
4. Настройте условия срабатывания
5. Добавьте канал уведомлений (email, Telegram, Slack)

### Elasticsearch запросы (для продвинутых)

**Адрес Elasticsearch:**
```
http://90.156.231.174:9200
```

⚠️ **Примечание:** Это тестовый сервер Shiwa для сбора телеметрии устройств.

**Пример запроса через curl:**
```bash
# Получить последние 10 записей от устройства TESTER
curl -X GET "http://90.156.231.174:9200/shiwatime-*/_search?pretty" -H 'Content-Type: application/json' -d'
{
  "query": {
    "bool": {
      "must": [
        { "match": { "name": "TESTER" } }
      ]
    }
  },
  "size": 10,
  "sort": [
    { "@timestamp": { "order": "desc" } }
  ]
}
'
```

**Просмотр индексов:**
```bash
curl -X GET "http://90.156.231.174:9200/_cat/indices/shiwatime-*?v"
```

### Проверка отправки данных

**На устройстве Timecard Mini:**
```bash
# Проверить, что данные отправляются
sudo journalctl -u shiwatime -f | grep -E "elasticsearch|output"
```

**Проверить доступность Elasticsearch:**
```bash
curl http://90.156.231.174:9200
```

Должен вернуться JSON с информацией о кластере.

### Устранение проблем с мониторингом

**Данные не отображаются в Grafana:**

1. Проверьте конфигурацию Elasticsearch на устройстве:
```bash
sudo grep -A 5 "output.elasticsearch" /etc/shiwatime/shiwatime.yml
```

Должно быть:
```yaml
output.elasticsearch:
  hosts: ['90.156.231.174:9200']
```

2. Проверьте, что сервис запущен:
```bash
sudo systemctl status shiwatime
```

3. Проверьте логи на ошибки отправки:
```bash
sudo journalctl -u shiwatime -n 100 | grep -i error
```

4. Убедитесь, что Elasticsearch доступен:
```bash
telnet 90.156.231.174 9200
```

**Большой лаг (задержка) в данных:**
- Проверьте сетевое подключение
- Убедитесь, что на устройстве достаточно ресурсов (CPU, память)
- Проверьте размер буфера в конфигурации

### Рекомендации по мониторингу

**Для операторов:**
- Проверяйте дашборды минимум 1 раз в день
- Настройте алерты для критических метрик
- Следите за трендом offset - он должен быть стабильным

**Пороговые значения:**
- **Offset**: ±50ns - отлично, ±100ns - хорошо, >500ns - требует внимания
- **Jitter**: <10ns - отлично, 10-50ns - нормально, >100ns - проблема
- **GNSS спутники**: >8 - отлично, 4-8 - приемлемо, <4 - плохо

**При проблемах:**
1. Проверьте GNSS антенну (вид на небо, кабель)
2. Проверьте сетевое подключение
3. Перезапустите сервис: `sudo systemctl restart shiwatime`
4. Проверьте логи: `sudo journalctl -u shiwatime -n 100`

---

## 🔒 Безопасность

### Изменение пароля пользователя

```bash
passwd
```

### Обновление системы

```bash
sudo apt update
sudo apt upgrade
```

### Настройка файрвола (опционально)

```bash
# Установка UFW
sudo apt install ufw

# Разрешить SSH
sudo ufw allow 22/tcp

# Включить файрвол
sudo ufw enable
```

---

## 📊 Мониторинг и диагностика

### Проверка производительности

```bash
# Использование CPU и памяти
top

# Или более современная альтернатива
htop

# Информация о системе
neofetch
```

### Диагностика проблем

Если сервис не запускается:

1. Проверьте логи:
```bash
sudo journalctl -u shiwatime -n 100 --no-pager
```

2. Проверьте конфигурацию:
```bash
sudo /usr/share/shiwatime/bin/shiwatime test config -c /etc/shiwatime/shiwatime.yml
```

3. Проверьте права доступа:
```bash
ls -la /etc/shiwatime/
ls -la /usr/share/shiwatime/bin/shiwatime
```

---

## 📞 Полезные ссылки

- **Документация:** https://shiwanetwork.tech/

---

## 🔄 Создание резервной копии конфигурации

```bash
# Создать резервную копию
sudo cp /etc/shiwatime/shiwatime.yml /etc/shiwatime/shiwatime.yml.backup

# Восстановить из резервной копии
sudo cp /etc/shiwatime/shiwatime.yml.backup /etc/shiwatime/shiwatime.yml
sudo systemctl restart shiwatime
```

---

## ⚡ Быстрые команды

```bash
# Полный перезапуск с просмотром статуса
sudo systemctl restart shiwatime && sudo systemctl status shiwatime

# Просмотр последних 20 строк логов
sudo journalctl -u shiwatime -n 20 --no-pager

# Редактирование + перезапуск
sudo nano /etc/shiwatime/shiwatime.yml && sudo systemctl restart shiwatime
```

---

## 🌐 Настройка PTP Grandmaster

### Включение PTP для раздачи времени

Shiwa Time может работать как PTP Grandmaster для раздачи точного времени по сети.

**1. Откройте конфигурацию:**
```bash
sudo nano /etc/shiwatime/shiwatime.yml
```

**2. Найдите секцию PTP в `primary_clocks` и настройте:**
```yaml
primary_clocks:
  # PTP Grandmaster - раздача времени
  - protocol:                 ptp
    domain:                   0
    serve_unicast:           true      # Включить unicast PTP
    max_unicast_subscribers: 20        # Максимум клиентов
    serve_multicast:         true      # Включить multicast PTP
    server_only:             true      # Только сервер (grandmaster mode)
    announce_interval:        1
    sync_interval:            0
    delayrequest_interval:    0
    disable:                  false    # ВАЖНО: false для включения!
    interface:               eth0      # Ваш сетевой интерфейс
    priority1:               64        # Приоритет (меньше = выше)
    priority2:               64
    profile:                 'enterprise-draft'
    
  # PPS - источник времени от GNSS
  - protocol:          pps
    interface:         eth0
    pin:               0
    index:             0
    cable_delay:       0
    edge_mode:         "rising"
    monitor_only:      false
    atomic:            false
    linked_device:     '/dev/ttyS0'
```

**3. Настройте Clock Quality (найдите секцию `clock_quality`):**
```yaml
ptp_tuning:
  clock_quality:
    auto:         false       # Отключить авто
    class:        6           # Class 6 = Primary Reference (GNSS)
    accuracy:     0x20        # 0x20 = 25ns точность
    variance:     0x4E20      # Variance
    timesource:   0x20        # 0x20 = GNSS time source
```

**4. Перезапустите сервис:**
```bash
sudo systemctl restart shiwatime
```

### Проверка работы PTP Grandmaster

**Проверить открытые PTP порты:**
```bash
sudo ss -ulnp | grep -E "319|320"
```
Должны быть видны порты 319 и 320.

**Захватить PTP трафик:**
```bash
sudo tcpdump -i eth0 -nn port 319 or port 320 -c 10
```

**Проверить параметры Grandmaster:**
```bash
sudo timeout 10 tcpdump -i eth0 -nn 'port 320 and dst 224.0.1.129' -c 3 -v 2>&1 | grep -E "gm clock class|time source|gm clock accuracy"
```

**Ожидаемые значения:**
- `gm clock class : 6` - Primary Reference
- `time source : 0x20` - GNSS
- `gm clock accuracy : 32` - 25 наносекунд

### Параметры Clock Class

| Class | Описание | Применение |
|-------|----------|------------|
| 6 | Primary Reference | GPS/GNSS синхронизирован (рекомендуется) |
| 7 | Primary Reference Holdover | Режим удержания после потери GNSS |
| 13 | Application Specific | Специфичный источник времени |
| 14 | Частично синхронизирован | Базовая синхронизация |
| 248 | Default | Не синхронизирован |

---

## 🌐 Настройка сети на Raspberry Pi через SSH

### Просмотр текущих настроек сети

**Просмотр сетевых интерфейсов:**
```bash
ip addr show
```

**Просмотр подключений NetworkManager:**
```bash
nmcli connection show
```

**Проверка состояния сетевых интерфейсов:**
```bash
ip link show
```

### Изменение IP-адреса на статический (NetworkManager)

Timecard Mini использует **NetworkManager** для управления сетью. Для настройки статического IP:

**1. Просмотрите список подключений:**
```bash
nmcli connection show
```

Вы увидите что-то вроде:
```
NAME                   UUID                                  TYPE      DEVICE 
Ethernet connection 1  8cf92943-97ec-46ff-8af5-7f17d4bbde3c  ethernet  eth0
```

**2. Установите статический IP для Ethernet (eth0):**
```bash
sudo nmcli connection modify "Ethernet connection 1" \
  ipv4.addresses 192.168.1.100/24 \
  ipv4.gateway 192.168.1.1 \
  ipv4.dns "8.8.8.8,8.8.4.4" \
  ipv4.method manual
```

**3. Применить изменения:**
```bash
sudo nmcli connection up "Ethernet connection 1"
```

**4. Проверьте новые настройки:**
```bash
ip addr show eth0
```

### Настройка Wi-Fi через NetworkManager

**Просмотр доступных Wi-Fi сетей:**
```bash
nmcli device wifi list
```

**Подключиться к Wi-Fi:**
```bash
sudo nmcli device wifi connect "Название_WiFi" password "Пароль"
```

**Настроить статический IP для Wi-Fi:**
```bash
sudo nmcli connection modify "Название_WiFi" \
  ipv4.addresses 192.168.1.101/24 \
  ipv4.gateway 192.168.1.1 \
  ipv4.dns "8.8.8.8,8.8.4.4" \
  ipv4.method manual
```

**Применить изменения:**
```bash
sudo nmcli connection up "Название_WiFi"
```

### Смена имени хоста (hostname)

**1. Изменить имя хоста:**
```bash
sudo hostnamectl set-hostname новое-имя
```

**2. Отредактировать файл /etc/hosts:**
```bash
sudo nano /etc/hosts
```

Измените строку:
```
127.0.1.1    raspberrypi
```
На:
```
127.0.1.1    новое-имя
```

**3. Перезагрузите для применения изменений:**
```bash
sudo reboot
```

### Настройка через raspi-config (альтернатива)

```bash
sudo raspi-config
```

Выберите:
- **System Options** → **Wireless LAN** для настройки Wi-Fi
- **System Options** → **Hostname** для смены имени
- **Advanced Options** → **Network Config** для выбора менеджера сети

### Проверка и диагностика

**Проверить соединение:**
```bash
ping -c 4 8.8.8.8
ping -c 4 google.com
```

**Проверить маршруты:**
```bash
ip route show
```

**Проверить DNS:**
```bash
cat /etc/resolv.conf
```

**Перезапустить сеть полностью:**
```bash
sudo systemctl restart networking
```

**Проверить статус NetworkManager:**
```bash
sudo systemctl status NetworkManager
```

### Пример: Смена IP с 192.168.16.238 на 192.168.1.100

**Шаг 1:** Подключитесь к устройству:
```bash
ssh shiwa@192.168.16.238
# Пароль: 278934
```

**Шаг 2:** Просмотрите текущие подключения:
```bash
nmcli connection show
```

**Шаг 3:** Измените IP на статический (замените "Ethernet connection 1" на имя вашего подключения):
```bash
sudo nmcli connection modify "Ethernet connection 1" \
  ipv4.addresses 192.168.1.100/24 \
  ipv4.gateway 192.168.1.1 \
  ipv4.dns "8.8.8.8,8.8.4.4" \
  ipv4.method manual
```

**Шаг 4:** Примените изменения:
```bash
sudo nmcli connection up "Ethernet connection 1"
```

**Шаг 5:** Проверьте новый IP:
```bash
ip addr show eth0 | grep "inet "
```

**Шаг 6:** Переподключитесь по новому IP:
```bash
exit
ssh shiwa@192.168.1.100
```

### ⚠️ Важные замечания

1. **Перед изменением IP убедитесь:**
   - Новый IP находится в той же подсети, что и ваш компьютер
   - IP не занят другим устройством
   - Правильно указан шлюз (обычно IP роутера)

2. **Если потеряли доступ:**
   - Подключите монитор и клавиатуру к Raspberry Pi
   - Исправьте конфигурацию напрямую
   - Или верните DHCP: `sudo nmcli connection modify "Ethernet connection 1" ipv4.method auto && sudo nmcli connection up "Ethernet connection 1"`

3. **Вернуть DHCP (автоматическое получение IP):**
   ```bash
   sudo nmcli connection modify "Ethernet connection 1" ipv4.method auto
   sudo nmcli connection up "Ethernet connection 1"
   ```

---

## 📜 Вспомогательные скрипты

В процессе настройки были созданы полезные скрипты:

### 1. Скрипт проверки PTP конфигурации

**Файл:** `check_ptp_config.sh`

Проверяет настройку PTP Grandmaster и показывает текущее состояние.

**Использование:**
```bash
chmod +x check_ptp_config.sh
sudo ./check_ptp_config.sh
```

**Что проверяет:**
- Настройки clock_quality в конфиге
- Статус сервиса shiwatime
- Открытые PTP порты (319, 320)
- PTP announce сообщения
- Последние логи сервиса

### 2. Системный service файл

**Файл:** `shiwatime.service`

Это файл systemd unit, который управляет запуском Shiwa Time.

**Текущее расположение:** `/lib/systemd/system/shiwatime.service`

**Содержимое:**
```ini
[Unit]
Description=Shiwa Time - PTP / NTP clock synchronisation system
Documentation=https://shiwanetwork.tech/
Wants=network-online.target
After=network-online.target

[Service]
Type=simple
Environment="BEAT_LOG_OPTS=-e"
Environment="BEAT_CONFIG_OPTS=-c /etc/shiwatime/shiwatime.yml"
Environment="BEAT_PATH_OPTS=--path.home /usr/share/shiwatime --path.config /etc/shiwatime --path.data /var/lib/shiwatime --path.logs /var/log/shiwatime"
ExecStart=/usr/share/shiwatime/bin/shiwatime --environment systemd $BEAT_LOG_OPTS $BEAT_CONFIG_OPTS $BEAT_PATH_OPTS
Restart=always

[Install]
WantedBy=multi-user.target
```

---

## 🔐 Учетные данные

**SSH доступ:**
- Пользователь: `shiwa`
- Пароль: `278934`
- IP-адрес: `192.168.16.238`

**CLI интерфейс Shiwa Time:**
- Порт: `65129`
- Пользователь: `admin`
- Пароль: `password` (рекомендуется изменить в конфиге)
- Подключение: `ssh -p 65129 admin@127.0.0.1` (с самого устройства)
