# Руководство по настройке длительности импульса PPS

Это руководство описывает, как изменять длительность импульса PPS (Pulse Per Second) в бинарнике shiwatime между значениями 5 мс и 100 мс.

## 📋 Содержание

1. [Описание](#описание)
2. [Требования](#требования)
3. [Установка длительности 5 мс](#установка-длительности-5-мс)
4. [Возврат длительности 100 мс](#возврат-длительности-100-мс)
5. [Проверка применения](#проверка-применения)
6. [Восстановление из резервной копии](#восстановление-из-резервной-копии)
7. [Устранение проблем](#устранение-проблем)
8. [Мониторинг через UBX команды](#мониторинг-через-ubx-команды)

---

## Описание

По умолчанию shiwatime отправляет на модуль Ublox конфигурацию с длительностью импульса PPS равной 100 мс. Для некоторых применений требуется более короткий импульс - 5 мс.

Скрипты:
- **`set_pps_5ms.py`** - устанавливает длительность импульса на 5 мс
- **`set_pps_100ms.py`** - возвращает длительность импульса на 100 мс (значение по умолчанию)

Оба скрипта работают с бинарником `/usr/share/shiwatime/bin/shiwatime`, заменяя значения в бинарном коде.

---

## Требования

1. **Права доступа:** Требуются права root (sudo)
2. **Файлы скриптов:** Скопируйте на устройство следующие файлы:
   - `set_pps_5ms.py`
   - `set_pps_100ms.py`
   - `check_pps_patch.py` (для проверки)
3. **Доступ к устройству:** SSH доступ или прямой доступ к терминалу

---

## Установка длительности 5 мс

### Шаг 1: Копирование скрипта на устройство

Если файл еще не скопирован на устройство, используйте один из способов:

**Через SCP:**
```bash
scp set_pps_5ms.py user@device_ip:/home/user/
```

**Через файловый менеджер:** Скопируйте файл через WinSCP, FileZilla или другой FTP/SCP клиент.

**Через прямой ввод:** Создайте файл на устройстве и вставьте содержимое:
```bash
nano set_pps_5ms.py
# Вставьте содержимое файла и сохраните (Ctrl+X, Y, Enter)
```

### Шаг 2: Установка прав на выполнение

```bash
chmod +x set_pps_5ms.py
```

### Шаг 3: Запуск скрипта

```bash
sudo python3 set_pps_5ms.py
```

### Пример вывода:

```
================================================================================
Установка длительности импульса PPS на 5 мс
================================================================================

Создание резервной копии: /usr/share/shiwatime/bin/shiwatime.backup
✓ Резервная копия создана

Поиск значения 100,000,000 (100 мс) в бинарнике...

  Найдено значение 100000000 (little-endian) по смещению 0x504E718
  Найдено значение 100000000 (little-endian) по смещению 0x504F2D0
  ...
✓ Заменено 16 вхождений
✓ Размер файла: 126,926,990 байт (не изменился)

================================================================================
✓ Установка длительности импульса на 5 мс выполнена успешно!
================================================================================

Следующие шаги:
1. Перезапустите shiwatime: sudo systemctl restart shiwatime
2. Проверьте логи: sudo journalctl -u shiwatime -f
3. Проверьте применение патча:
   sudo python3 check_pps_patch.py
4. Проверьте через мониторинг UBX команд:
   sudo python3 monitor_ubx_commands.py /dev/ttyS0 9600
```

### Шаг 4: Перезапуск shiwatime

```bash
sudo systemctl restart shiwatime
```

### Шаг 5: Проверка статуса

```bash
sudo systemctl status shiwatime
```

---

## Возврат длительности 100 мс

### Шаг 1: Копирование скрипта на устройство

Скопируйте `set_pps_100ms.py` на устройство (аналогично способу выше).

### Шаг 2: Установка прав на выполнение

```bash
chmod +x set_pps_100ms.py
```

### Шаг 3: Запуск скрипта

```bash
sudo python3 set_pps_100ms.py
```

### Пример вывода:

```
================================================================================
Возврат длительности импульса PPS на 100 мс
================================================================================

ℹ Резервная копия найдена: /usr/share/shiwatime/bin/shiwatime.backup
  (Можно использовать для восстановления)

Поиск значения 5,000,000 (5 мс) в бинарнике...

  Найдено значение 5000000 (little-endian) по смещению 0x504E718
  Найдено значение 5000000 (little-endian) по смещению 0x504F2D0
  ...
✓ Заменено 16 вхождений
✓ Размер файла: 126,926,990 байт (не изменился)

================================================================================
✓ Возврат длительности импульса на 100 мс выполнен успешно!
================================================================================
```

### Шаг 4: Перезапуск shiwatime

```bash
sudo systemctl restart shiwatime
```

---

## Проверка применения

Используйте скрипт `check_pps_patch.py` для проверки текущего состояния:

```bash
sudo python3 check_pps_patch.py
```

### Ожидаемый вывод при установленном патче (5 мс):

```
================================================================================
Проверка применения патча настройки длительности импульса PPS
================================================================================

✓ Файл найден: /usr/share/shiwatime/bin/shiwatime
  Размер: 126,926,990 байт (121.05 МБ)

✓ Резервная копия найдена: /usr/share/shiwatime/bin/shiwatime.backup

Поиск значения 5,000,000 (5 мс) в бинарнике...
  ✓ Найдено 16 вхождений

Поиск значения 100,000,000 (100 мс) в бинарнике...
  ✓ Значение 100,000,000 (100 мс) не найдено

================================================================================
РЕЗУЛЬТАТ ПРОВЕРКИ:
================================================================================
✓ Патч ПРИМЕНЕН

  - Значение 5 мс найдено в бинарнике
  - Значение 100 мс отсутствует
```

### Ожидаемый вывод при исходном состоянии (100 мс):

```
Поиск значения 5,000,000 (5 мс) в бинарнике...
  ✗ Значение 5,000,000 (5 мс) не найдено

Поиск значения 100,000,000 (100 мс) в бинарнике...
  ✓ Найдено 16 вхождений

✗ Патч НЕ ПРИМЕНЕН
```

---

## Восстановление из резервной копии

Если что-то пошло не так, можно восстановить оригинальный бинарник из резервной копии:

```bash
sudo cp /usr/share/shiwatime/bin/shiwatime.backup /usr/share/shiwatime/bin/shiwatime
sudo systemctl restart shiwatime
```

### Проверка восстановления:

```bash
sudo python3 check_pps_patch.py
```

Резервная копия создается автоматически при первом запуске `set_pps_5ms.py`.

---

## Мониторинг через UBX команды

Для проверки реальной конфигурации, отправляемой на модуль Ublox, используйте скрипт мониторинга:

```bash
sudo python3 monitor_ubx_commands.py /dev/ttyS0 9600
```

### Что искать в выводе:

При правильной настройке вы должны увидеть сообщения `CFG-TP5` (Time Pulse 5) с длительностью импульса:

**Для 5 мс:**
```
⏱️  TIMEPULSE конфигурация:
     Индекс TP: 0
     Активен: True
     Длительность импульса: 5.000 мс (5000000 нс)
```

**Для 100 мс:**
```
⏱️  TIMEPULSE конфигурация:
     Индекс TP: 0
     Активен: True
     Длительность импульса: 100.000 мс (100000000 нс)
```

**ВНИМАНИЕ:** Если длительность отличается от ожидаемой, скрипт выдаст предупреждение:
```
⚠️  ВНИМАНИЕ: Длительность импульса = 100.0 мс (требуется 5 мс)
```

Для остановки мониторинга нажмите `Ctrl+C`.

---

## Устранение проблем

### Проблема: "Ошибка: требуется права root (sudo)"

**Решение:** Запустите скрипт с sudo:
```bash
sudo python3 set_pps_5ms.py
```

### Проблема: "Файл /usr/share/shiwatime/bin/shiwatime не найден"

**Возможные причины:**
1. shiwatime не установлен
2. Файл находится в другом месте

**Решение:** Проверьте расположение бинарника:
```bash
which shiwatime
find /usr -name shiwatime 2>/dev/null
```

### Проблема: "Значение не найдено в бинарнике"

**Возможные причины:**
1. Патч уже применен (для `set_pps_5ms.py`) или значение уже установлено
2. Бинарник имеет другую версию
3. Значение хранится в другом формате

**Решение:**
1. Проверьте текущее состояние: `sudo python3 check_pps_patch.py`
2. Если скрипт сообщает "уже присутствует", изменения не требуются
3. Восстановите из резервной копии и попробуйте снова

### Проблема: shiwatime не запускается после изменения

**Решение:**
1. Восстановите из резервной копии:
   ```bash
   sudo cp /usr/share/shiwatime/bin/shiwatime.backup /usr/share/shiwatime/bin/shiwatime
   sudo systemctl restart shiwatime
   ```
2. Проверьте логи:
   ```bash
   sudo journalctl -u shiwatime -n 50
   ```

### Проблема: Импульс все еще имеет неправильную длительность

**Возможные причины:**
1. shiwatime не был перезапущен после изменения
2. Модуль Ublox кэширует конфигурацию
3. Конфигурация перезаписывается другими процессами

**Решение:**
1. Убедитесь, что shiwatime перезапущен: `sudo systemctl restart shiwatime`
2. Проверьте через мониторинг UBX команд, что правильная конфигурация отправляется
3. Проверьте логи shiwatime на наличие ошибок

---

## Быстрая справка

### Команды для установки 5 мс:
```bash
sudo python3 set_pps_5ms.py
sudo systemctl restart shiwatime
sudo python3 check_pps_patch.py
```

### Команды для возврата 100 мс:
```bash
sudo python3 set_pps_100ms.py
sudo systemctl restart shiwatime
sudo python3 check_pps_patch.py
```

### Команды для проверки:
```bash
# Проверка статуса патча
sudo python3 check_pps_patch.py

# Мониторинг UBX команд
sudo python3 monitor_ubx_commands.py /dev/ttyS0 9600

# Проверка логов shiwatime
sudo journalctl -u shiwatime -f
```

### Команда для восстановления:
```bash
sudo cp /usr/share/shiwatime/bin/shiwatime.backup /usr/share/shiwatime/bin/shiwatime
sudo systemctl restart shiwatime
```

---

## Важные замечания

1. **Всегда создавайте резервную копию** перед изменениями (скрипты делают это автоматически)
2. **Перезапускайте shiwatime** после каждого изменения
3. **Проверяйте результат** через `check_pps_patch.py` и мониторинг UBX команд
4. **Следите за логами** shiwatime после изменений
5. **Значение по умолчанию** - 100 мс. Если нужен короткий импульс, используйте 5 мс

---

## Дополнительная информация

- **Файл бинарника:** `/usr/share/shiwatime/bin/shiwatime`
- **Резервная копия:** `/usr/share/shiwatime/bin/shiwatime.backup`
- **Значение 5 мс:** 5000000 наносекунд (0x004C4B40)
- **Значение 100 мс:** 100000000 наносекунд (0x05F5E100)

Для получения помощи по UBX протоколу и конфигурации модулей Ublox, см.:
- `UBX_MONITORING_GUIDE.md`
- `configure_ublox_timepulse.py`
- Документацию Ublox по протоколу UBX
